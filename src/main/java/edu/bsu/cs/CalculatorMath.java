package edu.bsu.cs;

public class CalculatorMath extends App {
    public static void main(String args[]){
        int stage = 1;

        while (stage == 1) {
            //if # button pressed, add to string
            //if symbol button pressed, save variable with its function...
            //...take written number and convert to decimal, and save as variable, and stage = 2
            //(unless the sqrt or square, then just provide result there)

        }

        while (stage == 2) {
            //if # button pressed, add to new string
            //if equal button pressed, convert previous number to decimal...
            //then apply the two numbers based on saved symbol, and convert answer to quaternary
            //then, return that, and stage = 1
        }

    }
}

